# Meta Ads MCP Examples

This directory contains example scripts and usage demonstrations for the Meta Ads MCP server.

## Setup

Set the `META_ACCESS_TOKEN` environment variable with your Meta access token, then run the server:

```bash
python -m meta_ads_mcp
```

Or use the `install.sh` script for automated setup with Claude Desktop.
